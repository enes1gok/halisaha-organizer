/**
 * Shared layout constants for the floating pill tab bar + protruding FAB.
 * Keep scroll `paddingBottom` on tab-root screens in sync with these values.
 */
export const TAB_BAR_FLOAT_MARGIN_H = 16;
export const TAB_BAR_FLOAT_MARGIN_BOTTOM = 12;
/** Space reserved above the pill so the FAB half-circle is not clipped. */
export const TAB_BAR_FAB_OVERFLOW_TOP = 28;
export const TAB_BAR_FAB_SIZE = 56;

/** Bottom padding for FlatList/ScrollView content on main tab screens. */
export const TAB_BAR_LIST_PADDING_BOTTOM = 128;

/** Leaderboard: extra list padding when the pinned “you” strip is visible. */
export const TAB_BAR_LIST_PADDING_PINNED_EXTRA = 36;
