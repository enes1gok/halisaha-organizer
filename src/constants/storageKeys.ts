/** Cihaz yerel bayrakları (Zustand persist dışı). */
export const STORAGE_KEYS = {
  /** Ürün tanıtım slaytları en az bir kez tamamlandı veya atlandı. */
  appIntroCompleted: '@halisaha/app-intro-completed',
} as const;
